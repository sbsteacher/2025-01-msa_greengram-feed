<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.greengram.application.feedcomment.FeedCommentMapper">

    <select id="findAllByFeedIdLimitedTo">
        SELECT fc.feed_comment_id, fc.comment, fc.writer_user_id
          FROM feed_comment fc
         WHERE fc.feed_id = #{feedId}
         ORDER BY fc.feed_comment_id
         LIMIT #{startIdx}, #{sizePlusOne}
    </select>

</mapper>